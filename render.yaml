services:
  - type: web
    name: ztupid-gen
    env: node
    plan: free
    buildCommand: npm run install-all && npm run build
    startCommand: npm start
    healthCheckPath: /api/personalities
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000