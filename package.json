{
  "name": "voxel-ai",
  "version": "1.0.0",
  "description": "Advanced AI application with voxel rendering, multimedia processing, and voice assistant capabilities",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start": "npm run dev"
  },
  "keywords": ["ai", "voxel", "3d", "multimedia", "voice-assistant"],
  "author": "Voxel AI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}